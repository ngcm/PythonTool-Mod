machine:
  timezone:
    Europe/London
  # java:
  #   version: openjdk6

dependencies:
  override:
    - chmod u+x scripts/install-dependencies.sh
    - ./scripts/install-dependencies.sh
    - chmod +x scripts/build.sh
    - ./scripts/build.sh
    - scp forge-18/build/libs/pycraft-???.jar $DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH
test:
  override:
    - echo "All done"
