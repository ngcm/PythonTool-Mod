machine:
  timezone:
    Europe/London
  environment:
    _JAVA_OPTIONS: "-Xms2048m -Xmx3900m"

dependencies:
  override:
    - chmod u+x gradlew
    - ./gradlew setupDecompWorkspace
    - ./gradlew build
   
test:
  override:
    - echo "All done"

deployment:
  minecraft18:
    branch: minecraft19
    commands:
      - scp build/libs/pythontool-???.jar $DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH_19

