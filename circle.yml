machine:
  timezone:
    Europe/London
  # java:
  #   version: openjdk6

dependencies:
  override:
    - chmod u+x scripts/install-dependencies.sh
    - ./scripts/install-dependencies.sh
    - chmod +x scripts/build.sh
    - ./scripts/build.sh
   
test:
  override:
    - echo "All done"

deployment:
  minecraft194:
    branch: minecraft194
    commands:
      - scp forge/build/libs/pycraft-???.jar $DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH_194