machine:
  timezone:
    Europe/London
  environment:
    _JAVA_OPTIONS: "-Xms2048m -Xmx3900m"

dependencies:
  override:
    - chmod u+x scripts/install-dependencies.sh
    - ./scripts/install-dependencies.sh
    - chmod +x scripts/build.sh
    - ./scripts/build.sh
   
test:
  override:
    - echo "All done"

deployment:
  minecraft110:
    branch: minecraft110
    commands:
      - scp forge/build/libs/pycraft-???.jar $DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH_110
