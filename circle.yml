machine:
  timezone:
    Europe/London
  environment:
    _JAVA_OPTIONS: "-Xms2048m -Xmx3900m"

dependencies:
  override:
    - chmod u+x gradlew
    - ./gradlew setupDecompWorkspace
    - ./gradlew build
   
test:
  override:
    - echo "All done"

deployment:
  minecraft18:
    branch: minecraft110
    commands:
      - scp build/libs/pycraft-???.jar $DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_PATH_110

